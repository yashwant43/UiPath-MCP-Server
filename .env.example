# ── Authentication Mode ────────────────────────────────────────────────────────
# Choose one: cloud | on_prem | pat
AUTH_MODE=cloud

# ── Cloud OAuth2 (AUTH_MODE=cloud) ────────────────────────────────────────────
# Create an External Application in Automation Cloud > Admin > External Apps
# Grant scopes: OR.Jobs OR.Queues OR.Robots OR.Assets OR.Audit OR.Folders
UIPATH_CLIENT_ID=your_client_id_here
UIPATH_CLIENT_SECRET=your_client_secret_here
UIPATH_ORG_NAME=your_org_slug          # e.g. mycompany (from cloud.uipath.com/mycompany)
UIPATH_TENANT_NAME=DefaultTenant       # e.g. DefaultTenant

# ── On-Premise (AUTH_MODE=on_prem) ────────────────────────────────────────────
# UIPATH_BASE_URL=https://myserver.company.com/orchestrator
# UIPATH_USERNAME=admin@company.com
# UIPATH_PASSWORD=your_password
# UIPATH_TENANT_NAME=Default

# ── Personal Access Token (AUTH_MODE=pat) ─────────────────────────────────────
# UIPATH_BASE_URL=https://cloud.uipath.com/org/tenant/orchestrator_
# UIPATH_PAT=your_personal_access_token_here
# UIPATH_TENANT_NAME=DefaultTenant

# ── Optional ──────────────────────────────────────────────────────────────────
# UIPATH_FOLDER_ID=123          # Default folder/org-unit ID
# HTTP_TIMEOUT=30.0             # Request timeout in seconds
# RETRY_MAX_ATTEMPTS=3          # Max retry attempts on transient errors
# LOG_LEVEL=INFO                # DEBUG | INFO | WARNING | ERROR
# LOG_JSON=false                # true = structured JSON logs (for prod/log aggregators)
# MCP_TRANSPORT=stdio           # stdio | sse | streamable-http
# MCP_HOST=127.0.0.1            # Host for HTTP transport
# MCP_PORT=8000                 # Port for HTTP transport
